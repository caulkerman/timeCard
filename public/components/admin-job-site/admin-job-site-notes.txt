
Modal - Make it so the ng-hide and ng-show divs have the styling of a modal.

Modal - Make it so the late time event hides and shows with a button and make it look like a modal.
    
Modal - Make it (MAYBE) so the hidden-daily-time-card div looks like a modal with the date in its original position, the X to hide can be in the upper right corner like normal.
    
Highlight the "LATE" in red.

The comments in html says to make a spot for whether the timecard is T&M, because if it IS then there would be duplicate dates, and that would be ok if it's T&M.

Make it so there is a delete button in the parent ng-repeat to delete the entire daily_time_card if an employee accidently creates an empty time card, puts no employee time and/or materials etc in it. Make it so there is a warning so admin doesnt delete it accidently.

Make it so with the late time card that the admin can enter multiple employees and late time, updates immediately.

Make it so the materials_used and notes are editable, which means that they have their own update function.

Add total job site man hours to the jobsite details modal. /Create a function that adds all the hours up.

The createLateTimeCard function in the html needs to be moved to an button inside an element that hides and shows that is an input box where a date is to be entered. The button takes the inputted date and calles the function creating a new late daily time card.

